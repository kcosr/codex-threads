# Changelog

## [Unreleased]

### Added

- Initial `codex-threads` CLI for configured UDS Codex app-server targets.
- Thread list, search, show, messages, new, send, settings, status, control,
  model, and goal commands.
- Mock binary-level smoke tests and opt-in live smoke harness.
- Packaged `codex-threads-cli` assistant skill guidance for context discovery
  and thread control workflows.

### Changed

- Clarified `messages` help and docs for `--max-turns`, `--last`, role
  filtering, and older-history paging through `show`.
